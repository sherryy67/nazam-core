# Server Configuration
PORT=3001
NODE_ENV=development
HOST=localhost
EC2_PUBLIC_IP=18.215.151.243

# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017/nazam-core

# JWT Configuration
JWT_SECRET=your_jwt_secret_key_here
JWT_EXPIRE=7d

# Email Configuration (Microsoft 365/Outlook - GoDaddy Microsoft)
# Using info@zushh.com from GoDaddy Microsoft
EMAIL_USER=info@zushh.com
# IMPORTANT: For Microsoft 365, you may need to use an App Password instead of your regular password
# To create an App Password: account.microsoft.com/security > Advanced security options > App passwords
EMAIL_PASS=your_microsoft_365_app_password_here
EMAIL_FROM=info@zushh.com
EMAIL_HOST=smtp.office365.com
EMAIL_PORT=587
# Alternative variable names for backward compatibility
Email=info@zushh.com
Password=your_microsoft_365_app_password_here
EMAIL_PASSWORD=your_microsoft_365_app_password_here

# Admin Email (for receiving service request notifications)
ADMIN_EMAIL=info@zushh.com

# NOTE: If you get "SmtpClientAuthentication is disabled" error:
# 1. Contact GoDaddy support to enable SMTP authentication for your mailbox, OR
# 2. Use an App Password (recommended): account.microsoft.com/security > App passwords
# 3. Enable SMTP AUTH in Microsoft 365 Admin Center (if you have admin access)

# CORS Configuration (Optional - currently set to allow all origins)
# CORS_ORIGIN=http://localhost:3000

# AWS S3 Configuration
AWS_ACCESS_KEY_ID=your_aws_access_key_id
AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key
AWS_REGION=us-east-1
AWS_S3_BUCKET_NAME=your_s3_bucket_name

# SMS Configuration (Smart SMS Gateway)
SMS_USERNAME=facaltaasmkt
SMS_PASSWORD=6325hggds
SMS_SENDER_ID=AD-NAZAM

# Gemini AI Configuration
GEMINI_API_KEY=your_gemini_api_key_here

